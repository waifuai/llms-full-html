# Runtime dependencies (flexible pins suitable for OSS consumers)

# Google GenAI SDK (Client-centric API)
google-genai~=1.28
# HTTP client for OpenRouter integration
requests>=2.32.0,<3

# Notes:
# - Keep only direct runtime deps here for library/app consumers.
# - Dev/test pins are in requirements-dev.txt